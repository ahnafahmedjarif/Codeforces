#include <bits/stdc++.h>
using namespace std;

#define fastio ios::sync_with_stdio(0); cin.tie(0); cout.tie(0);
#define int long long

int32_t main() {
    fastio;

    string str;
    cin >> str;
    
    str[0] = toupper(str[0]);
    cout << str << endl;

    return 0;
}