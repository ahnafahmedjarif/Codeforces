#include <bits/stdc++.h>
using namespace std;

#define fastio ios::sync_with_stdio(0); cin.tie(0); cout.tie(0)
#define int long long

int32_t main(){
    fastio;

    int m, n;
    cin >> m >> n;
    cout << (m * n) / 2 << endl;

    return 0;
}